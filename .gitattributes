# Force LF for shell scripts so they work on macOS/Linux after checkout on Windows
*.sh    text eol=lf

# Windows scripts keep CRLF
*.ps1   text eol=crlf
*.bat   text eol=crlf

# Everything else: normalise to LF
*       text=auto
